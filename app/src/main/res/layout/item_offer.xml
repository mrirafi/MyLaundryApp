<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="220dp"
    android:layout_height="100dp"
    android:layout_margin="8dp"
    card_view:cardCornerRadius="16dp"
    card_view:cardElevation="4dp"
    android:foreground="?attr/selectableItemBackground">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp">

        <ImageView
            android:id="@+id/imgOffer"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_offer"
            android:layout_alignParentStart="true"
            android:contentDescription="Offer Icon" />

        <TextView
            android:id="@+id/tvOfferTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="10% Flat Discount"
            android:textStyle="bold"
            android:textSize="16sp"
            android:layout_toEndOf="@id/imgOffer"
            android:layout_marginStart="12dp"
            android:layout_alignTop="@id/imgOffer" />

        <TextView
            android:id="@+id/tvOfferSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="On all kinds of laundry services"
            android:textSize="14sp"
            android:layout_below="@id/tvOfferTitle"
            android:layout_alignStart="@id/tvOfferTitle"
            android:layout_marginTop="4dp" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>
